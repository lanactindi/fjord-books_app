<div class="col-md-8">
  <h3><%= User.human_attribute_name(:followings) %></h3>
  <% if @users.any? %>
    <ul class="users follow">
      <%= render partial: 'user', locals: {users: @users }%>
      <br/>
    </ul>
    <%= paginate @users %>
  <% end %>
</div>
