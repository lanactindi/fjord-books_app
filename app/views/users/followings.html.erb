<div class="col-md-8">
  <h3><%= t('views.common.followings') %></h3>
  <% if @users.any? %>
    <ul class="users follow">
      <%= render partial: 'user', locals: {users: @users }%>
      <br/>
    </ul>
    <%= paginate @users %>
  <% end %>
</div>
