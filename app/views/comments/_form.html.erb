<%= form_with(model: [commentable, comment= Comment.new], local: true) do |f| %>  
 <% if commentable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commentable.errors.count, "error") %> prohibited this commentable from being saved:</h2>

      <ul>
        <% commentable.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.text_area :content, required: true %>  

  <div class="actions">  
    <%= f.submit %>  
  </div>  
<% end %>