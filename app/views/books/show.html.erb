<p id="notice"><%= notice %></p>

<p>
  <strong><%= t("views.books.title") %></strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t("views.books.memo") %></strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t("views.books.author") %></strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t("views.books.picture") %></strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t("views.actions.edit"), edit_book_path(@book) %> |
<%= link_to t("views.actions.back"), books_path %>
